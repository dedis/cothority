syntax = "proto2";

import "network/network.proto";

package cothority;

message Request{

}

message Response {
    map<string, Status> system = 1;
    optional network.ServerIdentity server = 2;

    message Status {
        map<string, string> field = 1;
    }
}